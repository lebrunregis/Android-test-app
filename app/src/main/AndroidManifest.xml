<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evoliris.helloworld">
    <uses-sdk
        android:maxSdkVersion="25"
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACTION_MEDIA_SCANNER_SCAN_FILE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity3" />
        <activity android:name=".Activity1" />
        <activity android:name=".DrawActivity"/>
        <activity android:name=".TextEditorActivity" />
        <activity android:name=".PhoneActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".Activity5" />
        <activity
            android:name=".EditColor"
            android:label="Color options"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>